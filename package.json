{
  "name": "chrome-xxxx",
  "version": "0.2.1",
  "type": "module",
  "description": "Extension for black block",
  "homepage": "",
  "author": {
    "name": "uhyo",
    "email": "uhyo@uhy.ooo",
    "url": ""
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "keywords": [
    ""
  ],
  "devDependencies": {
    "@types/chrome": "^0.1.12",
    "@types/jest": "^30.0.0",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^9.36.0",
    "jest": "^30.1.3",
    "lint-staged": "^16.1.6",
    "prettier": "^1.19.1",
    "ts-jest": "^29.4.4",
    "typescript": "^5.9.2",
    "vite": "^7.1.6"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest",
    "lint": "eslint lib/**/*.ts --fix",
    "format": "prettier --write lib/**/*.{ts,tsx,js,jsx,json}",
    "type-check": "tsc --noEmit",
    "precommit": "lint-staged",
    "package": "npm run build && mkdir -p chrome-extension/app/icons && cp -r manifest.json js dist html _locales chrome-extension/app/ && cp icons/*.png chrome-extension/app/icons/ && cd chrome-extension/app && zip -r ../app.zip ."
  },
  "repository": "uhyo/chrome-xxxx",
  "license": "MIT",
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,jsx,json,md}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "my-key-config": "^0.1.2"
  }
}